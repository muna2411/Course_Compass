/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ["i.ibb.co", "assets.stickpng.com", "lh3.googleusercontent.com"],       
    },
    
};

export default nextConfig;
